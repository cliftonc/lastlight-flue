# --- Secrets (never commit) ---
secrets/
.env
.env.*
!.env.example
*.pem
*.key
*.private-key.*

# --- Node / build ---
node_modules/
# Build output (`flue build` → ./dist/server.mjs). Root-scoped so it does NOT
# ignore the committed prebuilt dashboard assets at `dashboard/dist/`.
/dist/
*.tsbuildinfo
.flue/
coverage/

# --- Local state / runtime ---
*.db
*.db-journal
*.sqlite
logs/
.DS_Store

# --- Editor ---
.idea/
.vscode/

# --- Claude Code harness (local) ---
.claude/
data/
